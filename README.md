# huggingface-nlp-inference
Hands-on implementation of NLP tasks using Transformers, including text classification and text generation with DistilBERT and GPT-2. Covers both manual inference (model loading, tokenization, output processing) and high-level pipeline usage for efficient deployment.
